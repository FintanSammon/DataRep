//Extending the arrow function that it now takes an argument and returns the passed variable. 
//Assigning the arrow function equal to a variable and getting the function to output to the console.

const PartB = (myValue)=>{
    return myValue;
}

console.log(PartB("Hello from partB"));